function [features] = joinFeaturesLooks(looks, noLooks)
    features = horzcat(                   ...
        buildMeanFeature(looks, noLooks), ...
        buildHOGFeature(looks, noLooks)   ...            
    );
end

